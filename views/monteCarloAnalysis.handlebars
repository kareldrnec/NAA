{{> navbar_index}}

{{#if flash}}
    <div style="text-align: center; position: absolute; width: 100%;" id="flash" class="alert alert-{{flash.type}}" role="alert">
        <button type="button" class="close" data-dismiss="alert">&times;</button>
        <strong>{{flash.text}}</strong>
    </div>
{{/if}}

<div class="container-fluid bg pt-5">
    <div class="row">
        <div class="col-md-4 col-sm-4 col-xs-12">
            <form class="form-container" action="">
                <div class="text-center">
                    <h3>{{__ "simulation"}}</h3>
                </div>
                <div class="form-group">
                    <label for="numberOfIterations">{{__ "number of iterations"}}</label>
                    <input type="number" class="form-control" id="numberOfIterations" placeholder="{{__ "enter number of iterations"}}" min="10" value="10">
                </div>
                <div class="text-center">
                    <button type="button" class="btn btn-primary" onclick="runMonteCarlo();">{{__ "run simulation"}}</button>
                    <button type="button" class="btn btn-secondary" onclick="history.back()">{{__ "back"}}</button>
                </div>
            </form>
            <div style="background-color: #ffb8b8; margin-top: 10px; display: none;" id="monteCarloErrDiv">
                <button type="button" class="close" onclick="closeMonteCarloErr()" style="margin-right: 5px;">
                    <span aria-hidden="true">&times;</span>
                </button>
                <div id="monteCarloErrMsg" style="margin-left: 5px;">
                </div>
            </div>
        </div>
        <div class="col-md-8 col-sm-8 col-xs-24">
            <canvas id="myChart" width="800px" height="300px"></canvas>
            <div class="text-center">
                <button class="btn btn-primary">{{__ "download graph"}}</button>
            </div>
        </div>
    </div>
</div>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/3.6.0/chart.min.js"
    integrity="sha512-GMGzUEevhWh8Tc/njS0bDpwgxdCJLQBWG3Z2Ct+JGOpVnEmjvNx6ts4v6A2XJf1HOrtOsfhv3hBKpK9kE5z8AQ=="
    crossorigin="anonymous" referrerpolicy="no-referrer"></script>
<script src="/javascripts/calculations.js"></script>
<script src="/javascripts/jstat.min.js"></script>
<script>

    var activities = JSON.parse(sessionStorage.getItem("activities"));
    var states = JSON.parse(sessionStorage.getItem("states"));


    function runMonteCarlo() {
       // console.log(checkDiagram(states, activities));

    // simulationMonteCarlo(numberOfIterations, states, activities)
        const numberOfIterations = parseInt(document.getElementById("numberOfIterations").value);

        console.log("Jde se na monte carlo")
        //simulateMonteCarlo(numberOfIterations, states, activities);



        if (window.Worker) {
            var myWorker = new Worker('worker.js');
            console.log("JDE UZIT WORKER")
        } else {
            console.log("NEJDE")
        }


    }

    //
    function closeMonteCarloErr() {
        document.getElementById('monteCarloErrDiv').style.display = 'none';
    }



    const ctx = document.getElementById('myChart').getContext('2d');
    const myChart = new Chart(ctx, {
        type: 'line',
        data: {
            labels: ['Red', 'Blue', 'Yellow', 'Green', 'Purple', 'Orange'],
            datasets: [{
                label: '# of Votes',
                data: [12, 19, 3, 5, 2, 3],
                backgroundColor: 'rgba(255, 99, 132, 0.2)',
                borderColor: 'rgba(255, 99, 132, 1)',
                borderWidth: 1
            }]
        },
        options: {
            scales: {
                y: {
                    beginAtZero: true
                }
            }
        }
    });

</script>